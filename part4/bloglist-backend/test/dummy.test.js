const listHelper = require('../utils/list_helper')

test('dummy returns one', () => {
  const blogs = []
  if (listHelper === 5) { return }
  const result = listHelper.dummy(blogs)
  expect(result).toBe(1)
})